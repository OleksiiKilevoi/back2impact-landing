# Google Apps Script web app URL that receives waitlist signups.
# Copy this file to `.env` and paste your deployed web app URL.
# (This URL is called from the browser, so it is NOT secret.)
VITE_WAITLIST_ENDPOINT=https://script.google.com/macros/s/XXXXXXXXXXXXXXXXXXXX/exec
